# `hopsworks-rs` Snippets

A collection of helpful template to accelerate your development with `hopsworks-rs`. Use them as reference, template for your own scripts or elementary blocks to build more complex pipelines.
